# Run Fontforge in Docker
This repo contains a Dockerfile to build a Docker image used for running Fontforge. 

## Usage
- docker pull emielmolenaar/fontforge
- docker run -it --rm emielmolenaar/fontforge <arguments>
